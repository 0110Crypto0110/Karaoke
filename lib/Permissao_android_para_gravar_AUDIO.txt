<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <uses-feature android:name="android.hardware.microphone" android:required="true"/>
    
    <application 
        android:label="flutter_karaoke_analyzer" 
        android:name="${applicationName}">
        ```

### B. Para iOS (Privacidade do Microfone)

Adicione a descrição de uso do microfone no arquivo `Info.plist`.

**Arquivo:** `ios/Runner/Info.plist`

**Localização:** Adicione as chaves (`<key>` e `<string>`) dentro da tag principal `<dict>`.

```xml
<dict>
    <key>NSMicrophoneUsageDescription</key>
    <string>Este aplicativo precisa de acesso ao microfone para gravar sua performance de karaokê.</string>
    
    </dict>